const nome = "silvana";
const sexo = "F";
const idade = 48;
const contribuicao = 23;

if ((sexo == "f") && (contribuicao >= 35)){ 
    if ((contribuicao + idade))
  }else {
    console.log("Seu tempo de contribuição ainda não atingiu o minimo necessário")
  }
}else{
  
}